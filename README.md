Dependencies :

* git
* tmux
* vim
* zsh


To install lc-shell :

```console
$ mkdir "${HOME}/lc-shell"
$ git clone https://github.com/lightcode/lc-shell.git "${HOME}/lc-shell"
$ cd "${HOME}/lc-shell"
$ ./setup.sh
```
